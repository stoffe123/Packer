#ifndef CANONICAL_HEADER_PACKER_H
#define CANONICAL_HEADER_PACKER_H

int canonical_header_pack(const char* source_filename, const char* dest_filename);

int canonical_header_unpack(const char* source_filename, const char* dest_filename);


#endif